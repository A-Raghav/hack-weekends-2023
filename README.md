# hack-weekends-2023
A language translation model which will convert the mappings and transformations rules defined in CSV data directly into ready-to-run SQL queries, saving time taken to on-board new-customers, or even convert new payers data into standard data models.
